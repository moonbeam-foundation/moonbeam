# Workspace-root install (CI): applies to `pnpm install` from repo root.
# Prefer fewer parallel registry connections on flaky / bare-metal runners.
network-concurrency=4
fetch-timeout=600000
fetch-retries=10
