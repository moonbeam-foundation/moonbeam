initSidebarItems({"struct":[["IncreaseMaxDelegationsPerCandidate","Migration to properly increase maximum delegations per collator This migration can be used to recompute the top and bottom delegations whenever MaxDelegatorsPerCandidate changes (works for decrease as well)"],["PurgeStaleStorage","Migration to purge staking storage bloat for `Points` and `AtStake` storage items"],["RemoveExitQueue","Migration to replace the automatic ExitQueue with a manual exits API. This migration is idempotent so it can be run more than once without any risk."]]});