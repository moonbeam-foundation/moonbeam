diff --git a/dist/cjs/blockchain/block.js b/dist/cjs/blockchain/block.js
index d751a928b5c04a4fac2d9bb3e69df09b8d65187c..46278fd8b07393969eacad52f20f1f58e1362df1 100644
--- a/dist/cjs/blockchain/block.js
+++ b/dist/cjs/blockchain/block.js
@@ -67,7 +67,7 @@ function _define_property(obj, key, value) {
     }
     return obj;
 }
-var _chain = /*#__PURE__*/ new WeakMap(), _header = /*#__PURE__*/ new WeakMap(), _parentBlock = /*#__PURE__*/ new WeakMap(), _extrinsics = /*#__PURE__*/ new WeakMap(), _wasm = /*#__PURE__*/ new WeakMap(), _runtimeVersion = /*#__PURE__*/ new WeakMap(), _metadata1 = /*#__PURE__*/ new WeakMap(), _registry = /*#__PURE__*/ new WeakMap(), _meta = /*#__PURE__*/ new WeakMap(), _baseStorage = /*#__PURE__*/ new WeakMap(), _storages = /*#__PURE__*/ new WeakMap();
+var _chain = /*#__PURE__*/ new WeakMap(), _header = /*#__PURE__*/ new WeakMap(), _parentBlock = /*#__PURE__*/ new WeakMap(), _extrinsics = /*#__PURE__*/ new WeakMap(), _wasm = /*#__PURE__*/ new WeakMap(), _runtimeVersion = /*#__PURE__*/ new WeakMap(), _metadata1 = /*#__PURE__*/ new WeakMap(), _registry = /*#__PURE__*/ new WeakMap(), _meta = /*#__PURE__*/ new WeakMap(), _extrinsicMeta = /*#__PURE__*/ new WeakMap(), _baseStorage = /*#__PURE__*/ new WeakMap(), _storages = /*#__PURE__*/ new WeakMap();
 class Block {
     get chain() {
         return _class_private_field_get(this, _chain);
@@ -240,6 +240,15 @@ class Block {
         }
         return _class_private_field_get(this, _meta);
     }
+    get extrinsicMeta() {
+        if (!_class_private_field_get(this, _extrinsicMeta)) {
+            _class_private_field_set(this, _extrinsicMeta, (async ()=>{
+                const parent = await this.parentBlock;
+                return parent ? parent.meta : this.meta;
+            })());
+        }
+        return _class_private_field_get(this, _extrinsicMeta);
+    }
     /**
    * Call a runtime method.
    */ async call(method, args, mockSigantureHostOverride = false) {
@@ -307,6 +316,10 @@ class Block {
             writable: true,
             value: void 0
         });
+        _class_private_field_init(this, _extrinsicMeta, {
+            writable: true,
+            value: void 0
+        });
         _class_private_field_init(this, _baseStorage, {
             writable: true,
             value: void 0
@@ -327,6 +340,7 @@ class Block {
         _class_private_field_set(this, _metadata1, parentBlock?.metadata);
         _class_private_field_set(this, _registry, parentBlock?.registry);
         _class_private_field_set(this, _meta, parentBlock?.meta);
+        _class_private_field_set(this, _extrinsicMeta, parentBlock?.meta);
         const storageDiff = block?.storageDiff;
         if (storageDiff) {
             // if code doesn't change then keep parent block's meta
@@ -336,6 +350,8 @@ class Block {
                 _class_private_field_set(this, _metadata1, undefined);
                 _class_private_field_set(this, _registry, undefined);
                 _class_private_field_set(this, _meta, undefined);
+                // _extrinsicMeta intentionally NOT reset: the block's extrinsics were
+                // encoded with the pre-upgrade runtime (parent's metadata).
             }
             this.pushStorageLayer().setAll(storageDiff);
         }
diff --git a/dist/cjs/blockchain/inherent/parachain/validation-data.js b/dist/cjs/blockchain/inherent/parachain/validation-data.js
index f904469464d323bf160a9b8a42e434496b9d30a1..c849c1498ba6846e4f1b11a1c4baadf219d57c7f 100644
--- a/dist/cjs/blockchain/inherent/parachain/validation-data.js
+++ b/dist/cjs/blockchain/inherent/parachain/validation-data.js
@@ -52,7 +52,6 @@ const MOCK_VALIDATION_DATA = {
     downwardMessages: []
 };
 const getValidationData = async (parent, fallback = true)=>{
-    const meta = await parent.meta;
     if (parent.number === 0) {
         const { trieRootHash, nodes } = await (0, _index1.createProof)(MOCK_VALIDATION_DATA.relayChainState.trieNodes, []);
         return {
@@ -68,6 +67,7 @@ const getValidationData = async (parent, fallback = true)=>{
     }
     try {
         const extrinsics = await parent.extrinsics;
+        const meta = await parent.extrinsicMeta;
         const validationDataExtrinsic = extrinsics.find((extrinsic)=>{
             const firstArg = meta.registry.createType('GenericExtrinsic', extrinsic)?.args?.[0];
             return firstArg && 'validationData' in firstArg;
diff --git a/dist/esm/blockchain/block.js b/dist/esm/blockchain/block.js
index edff7f74638a5c21c5f73c6831708b46eb6a576c..e040be84b2ef6bb6de63eea9b68729907a5945d0 100644
--- a/dist/esm/blockchain/block.js
+++ b/dist/esm/blockchain/block.js
@@ -31,6 +31,7 @@ import { RemoteStorageLayer, StorageLayer, StorageValueKind } from './storage-la
     #metadata;
     #registry;
     #meta;
+    #extrinsicMeta;
     #baseStorage;
     #storages;
     constructor(chain, number, hash, parentBlock, block){
@@ -46,6 +47,7 @@ import { RemoteStorageLayer, StorageLayer, StorageValueKind } from './storage-la
         this.#metadata = parentBlock?.metadata;
         this.#registry = parentBlock?.registry;
         this.#meta = parentBlock?.meta;
+        this.#extrinsicMeta = parentBlock?.meta;
         const storageDiff = block?.storageDiff;
         if (storageDiff) {
             // if code doesn't change then keep parent block's meta
@@ -55,6 +57,8 @@ import { RemoteStorageLayer, StorageLayer, StorageValueKind } from './storage-la
                 this.#metadata = undefined;
                 this.#registry = undefined;
                 this.#meta = undefined;
+                // #extrinsicMeta intentionally NOT reset: the block's extrinsics were
+                // encoded with the pre-upgrade runtime (parent's metadata).
             }
             this.pushStorageLayer().setAll(storageDiff);
         }
@@ -230,6 +234,15 @@ import { RemoteStorageLayer, StorageLayer, StorageValueKind } from './storage-la
         }
         return this.#meta;
     }
+    get extrinsicMeta() {
+        if (!this.#extrinsicMeta) {
+            this.#extrinsicMeta = (async ()=>{
+                const parent = await this.parentBlock;
+                return parent ? parent.meta : this.meta;
+            })();
+        }
+        return this.#extrinsicMeta;
+    }
     /**
    * Call a runtime method.
    */ async call(method, args, mockSigantureHostOverride = false) {
diff --git a/dist/esm/blockchain/inherent/parachain/validation-data.js b/dist/esm/blockchain/inherent/parachain/validation-data.js
index cff4abd9c9ce1c0967b95ca28789a6023bdc0bcc..0a2a70750519f4ac50ae5dffce405ce491778760 100644
--- a/dist/esm/blockchain/inherent/parachain/validation-data.js
+++ b/dist/esm/blockchain/inherent/parachain/validation-data.js
@@ -37,7 +37,6 @@ const MOCK_VALIDATION_DATA = {
     downwardMessages: []
 };
 const getValidationData = async (parent, fallback = true)=>{
-    const meta = await parent.meta;
     if (parent.number === 0) {
         const { trieRootHash, nodes } = await createProof(MOCK_VALIDATION_DATA.relayChainState.trieNodes, []);
         return {
@@ -53,6 +52,7 @@ const getValidationData = async (parent, fallback = true)=>{
     }
     try {
         const extrinsics = await parent.extrinsics;
+        const meta = await parent.extrinsicMeta;
         const validationDataExtrinsic = extrinsics.find((extrinsic)=>{
             const firstArg = meta.registry.createType('GenericExtrinsic', extrinsic)?.args?.[0];
             return firstArg && 'validationData' in firstArg;
